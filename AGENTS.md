# DcElysion 协作约定

Astro + Svelte 博客，使用 Swup 局部导航。依赖版本与命令以 `package.json` 为准；使用项目固定的 pnpm，Windows 可通过 `corepack pnpm` 调用。

## 工作范围与完成条件

- 修改前查看工作区状态并保留任务外改动；直接阅读受影响的实现，涉及共享配置、事件或接口时再追踪调用方。无需为小改动通读仓库或全部参考文档。
- 在已授权范围内完成实现、相关验证和由本次变更引起的修复，达到可检查的结果后交付。只读检查及不访问生产、不破坏持久数据的本地验证可以连续执行；遇到真实授权边界或缺失的关键输入才暂停依赖它的操作。
- 用能覆盖实际风险的验证判断完成。文档只检查内容、差异和引用；代码检查受影响的类型和行为；生成流程或生产特有行为再做完整构建。通过后，仅因新变更、失败或未解决风险追加检查。
- 文档事实因本次变更失真时，同步对应章节；不要求每次改动检查全部语言或无关架构文档。
- 简要说明结果、验证证据和影响结论的限制，不把环境失败当作验证通过。

## 项目约束

- 保持用户给出的正文、标点、表情和图片顺序；未要求时不润色。局部视觉调整限定作用范围。
- 上游同步带来的教学、指南和功能示例文章保持 `draft: true`，只有明确要求公开时才改变；个人正式文章不受此默认规则限制。
- 交互修改须考虑 Swup 重入与清理，保留现有容器 ID 和事件契约；相关细节见架构参考。
- 使用既有配置类型与模块约定。配置结构或事件契约变化时同步读取方；用户可见文案沿用 i18n。
- 不手改 `dist/`、`.astro/` 或依赖产物。构建可能改写受版本控制的缓存与 LQIP，提交前只保留任务相关差异。
- `pnpm lint` / `pnpm format` 会全目录写入；局部只读检查使用 `pnpm exec biome check <相关文件>`。
- 不提交密钥、真实密码或 `.env`；未经明确要求不运行会移动内容的 `scripts/quarantine-bad-posts.mjs`。

## Git 与发布边界

- 未获 Git 提交或推送授权时，先完成本地结果，再询问缺失的发布决定；已有本任务明确授权时无需重复询问。暂存和提交仅包含本任务文件，使用 Conventional Commits。
- OpenAI Sites 默认不部署。GitHub 提交或推送不构成 Sites 授权；执行 Sites source sync、版本保存或部署前，每次都必须单独询问并获得用户明确确认。
- 分别报告 Git 提交/推送、自动部署、Sites 同步/版本/部署和线上验证的实际状态。

## 按任务查阅

下面均为按需参考，不是开工必读清单。文档中的源码路径以仓库根目录为基准。

| 任务 | 参考 |
| --- | --- |
| 路由、配置、组件、排版、Swup、滚动或布局 | [架构与浏览器行为](docs/agent-reference/architecture.md)；按受影响模块选择小节 |
| 依赖、构建、缓存、字体、搜索或验证选择 | [构建与验证](docs/agent-reference/build.md) |
| 音乐、视频、相册、R2 或媒体域名 | [媒体约定](docs/agent-reference/media.md) |
| 新内容、页面、Widget、Markdown 或文档维护 | [工作流参考](docs/agent-reference/workflows.md) |
| 创建或修改动态及附件 | [.agents/skills/publish-dynamic/SKILL.md](.agents/skills/publish-dynamic/SKILL.md) |
